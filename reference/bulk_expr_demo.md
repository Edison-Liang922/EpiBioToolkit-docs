# Demo Bulk Expression Data

A demo bulk RNA-seq expression matrix for bulk workflows.

## Usage

``` r
bulk_expr_demo
```

## Format

A numeric matrix/data frame with genes in rows and samples in columns.

## Details

Rows are samples and columns are genes in this demo. Functions that
require genes in rows can transpose via \`t(bulk_expr_demo)\`.
